package cl.udla.ia.domain;

import java.util.List;

public interface EstructuraDatos<T implements EstructuraDatos,W> {
	
	void insert(W value);
	void remove(W value);
	T find(W key);
	List<T> get();

}
